# 이 저장소에서 작업할 때

수도권 실거주 아파트 후보를 공공데이터로 추출해 대시보드로 비교하는 도구다.
사용자는 실제로 집을 구하는 중이고, 이 결과를 보고 판단한다. **틀린 숫자가
그럴듯하게 보이는 것이 가장 큰 위험**이다.

먼저 `README.md`(사용법·조건)를 읽고, 이 문서(설계 의도·함정)를 읽어라.

## 시작 전 확인

```bash
python3 -m pytest -q            # 332개 통과해야 정상
node tools/verify_dashboard.mjs # 대시보드 열 정합성 (아래 참고)
```

동작에 필요한 것들 — 없으면 해당 단계가 건너뛰어지거나 멈춘다:

| 필요한 것 | 없으면 |
|---|---|
| `config.yaml` (API 키 4개) | 즉시 중단. `config.example.yaml` 복사해서 채운다 |
| 한국부동산원 단지 식별정보 CSV (44MB, 저장소에 없음) | 세대수·주상복합 필터 동작 안 함 |
| data.go.kr 활용신청 3종 (실거래가·단지목록·단지기본정보) | 해당 단계만 경고 후 건너뜀 |
| 카카오 앱의 "카카오맵" 제품 활성화 | 지오코딩 전체 실패 (403) |

## 데이터 소스와 각각의 역할

같은 정보를 여러 곳에서 찾아 헤매기 쉬운 구조다. **어느 소스가 무엇을 담당하는지
헷갈리면 시간을 크게 낭비한다.**

| 소스 | 담당 | 대체 불가한 이유 |
|---|---|---|
| 국토부 실거래가 API | 가격 | 아파트/주상복합 구분 필드가 **없다** |
| 한국부동산원 CSV (파일, 활용신청 불필요) | 세대수·동수·사용승인일·단지명 별칭 | **지번 주소로 실거래와 100% 매칭**된다 |
| 서울시 `OpenAptInfo` | 주상복합 판별·좌표·주차대수 | **서울 전용** (2,885단지) |
| 국토부 `AptListService4` + `AptBasisInfoServiceV5` | 서울 밖 주상복합 판별 | 목록으로 단지코드를 얻어야 기본정보를 조회할 수 있다 |
| 서울 열린데이터 `subwayStationMaster` | 역 좌표 | **수도권 전체** 784역 (서울 전용 아님) |
| 서울 열린데이터 `SearchSTNBySubwayLineInfo` | 노선 내 역 순서(`FR_CODE`) | 이걸로 지하철 그래프를 만든다 |
| 나이스 | 초·중·고 위치 | 시도교육청 코드별 (B10 서울, J10 경기) |
| 카카오 로컬 | 지오코딩·병원·마트·시장·학원 | 전국 동작 |
| 카카오모빌리티 | 자차 소요시간 | 추가 신청 없이 같은 REST 키로 동작 |

**주상복합 구분은 실거래·부동산원 어디에도 없다.** 서울은 `OpenAptInfo`,
서울 밖은 `AptBasisInfoServiceV5`만 가능하다.

## 파이프라인 순서는 비용 기준이다

`02_analyze.py`의 단계 순서를 바꾸면 API 호출이 폭증한다.

```
가격·면적·거래건수  (무료)      4,575곳
세대수·단지매칭     (무료)      2,501곳   ← 유료 호출 전에 최대한 줄인다
지오코딩            (카카오)
거리·통근           (카카오모빌리티)  620곳
단지분류            (data.go.kr, 일 한도 있음)  ← 반드시 마지막 필터 뒤
편의시설            (카카오)     475곳
```

한때 단지분류를 거리 필터보다 앞에서 호출해 2,537건을 조회하려다 한도를
넘길 뻔했다. 지금은 수십 건만 쓴다 (최근 실행 30건).

## API 쿼터

| API | 일 한도 | 이 도구 1회 실행 |
|---|---:|---:|
| 카카오 로컬 (지오코딩·편의시설) | 30,000 | 약 1,500 |
| 카카오모빌리티 (자차) | **5,000** | 약 2,000 |
| data.go.kr 실거래가 | (7년 수집 시 3,834건 성공) | 캐시 후 증분만 |

모든 조회는 디스크에 캐시된다. 재실행 비용은 거의 0이다.
**캐시 키를 바꾸면 전량 재조회된다** — 아래 함정 참고.

## 깨지기 쉬운 지점 (실제로 발생한 버그)

### 1. 조용히 실패하는 문자열 치환 — 두 번 당했다
템플릿의 `<th>`를 추가하는 치환이 줄바꿈 차이로 매칭되지 않아 **아무 일도
일어나지 않았다.** `<td>`만 추가되어 표 전체가 한 칸씩 밀렸고, 마지막
"매물확인" 칸에 거리 수치가 나왔다.

- 치환 후 **반드시 결과를 확인**하라. `assert old in text` 없이 replace 하지 마라.
- `tests/test_report.py::TestTableIntegrity`가 헤더/셀 개수를 검사한다.
- `node tools/verify_dashboard.mjs`가 **JS로 생성되는 그룹 열까지** 실제 실행해 센다.
  파이썬 테스트는 정적 HTML만 보므로 이것 없이는 못 잡는다.

### 2. serviceKey 이중 인코딩 (도구 전체가 죽었던 원인)
공공데이터포털 키를 `urlencode()`에 함께 넣으면 이미 인코딩된 키가 다시
인코딩되어 **전부 403**이 난다. `http.encode_service_key()`가 정확히 1회만
인코딩한다. 회귀 테스트가 있으니 지우지 마라.

### 3. data.go.kr의 오해를 부르는 오류 메시지
미승인 서비스도 `해당 오픈API 서비스가 없거나 폐기됨`(코드 12)을 반환한다.
**경로가 틀린 게 아니라 활용신청이 안 된 것**이다. 승인된 서비스는
`SERVICE_KEY_IS_NOT_REGISTERED`(코드 30)로 다른 오류를 낸다 — 이걸로 구분하라.

### 4. 좌표만으로 단지를 매칭하면 오분류한다
휘경제이스카이(아파트)가 91m 떨어진 "회기역 하트리움"(주상복합)에 붙어
**멀쩡한 아파트가 주상복합으로 걸러질 뻔했다.** `matching.py`는 이름 유사도를
먼저 통과한 후보만 거리로 고른다. 전국 조회에서는 지번 주소까지 대조한다.

### 5. 이름 유사도에서 포함관계를 1.0으로 주면 안 된다
"정자"가 "정자타워"에 들어간다고 같은 단지로 보면 안 된다. 길이 비율을
반영한다(`CONTAINMENT_BASE`). 완전일치만 1.0이다.

### 6. 지역 표기가 데이터마다 다르다
- 시군구: `성남시 분당구` / `성남분당구` / `수원장안구` → `normalize_district()`로 시·군·구 글자 제거
- 시도: `서울` / `서울특별시` 혼재 → `aptinfo.normalize_sido()`
- 역명: `경복궁` / `경복궁(정부서울청사)` → `transit.normalize_station()`
- **이름 대신 시군구코드(`sgg_code`)로 맞추는 게 가장 안전하다.**

### 7. 폐지·분리된 시군구 코드
화성시(41590)는 분구되어 0건을 반환한다. 41593/41595/41597을 쓴다.
부천시(41190)도 41192/41194/41196로 나뉘어 있다.
**코드를 추가하면 실제로 응답이 오는지 먼저 확인하라.**

### 8. 캐시 스키마를 바꾸면 재수집이 필요하다
`aptinfo`에 `sido` 필드를 추가했는데 캐시가 이전 버전이라 `classified_regions`가
빈 집합이 되었고, 주상복합 필터가 조용히 무력화됐다.
**파싱 결과에 필드를 추가하면 해당 캐시 파일을 지워라.**

### 9. 캐시 키에 날짜를 넣지 마라
자차 캐시 키에 조회 날짜가 있어 **날이 바뀔 때마다 4,257건 전량 재조회**됐다.
지금은 좌표쌍만 키로 쓰고 기준 시각은 값 안에 기록한다.

### 10. `.gitignore` 함정 두 가지
- `dashboard.html`은 경로를 가리지 않아 **`aptfinder/templates/dashboard.html`까지 제외**한다.
  루트만 막으려면 `/dashboard.html`.
- **줄 끝 주석을 지원하지 않는다.** `/dashboard.html  # 설명`은 그 전체가 패턴이 된다.

### 11. macOS 한글 파일명은 NFD다
`glob("*공동주택 단지 식별정보*.csv")`가 매칭되지 않는다.
`unicodedata.normalize("NFC", ...)`로 비교한다 (`reb.find_csv()`).

### 12. 지역별 데이터 커버리지가 다르면 필터를 전 지역에 적용하지 마라
`require_type_known`(분류 미확인 배제)을 전 지역에 걸면 분류 자료가 없는
경기 후보가 **통째로 사라진다**. `classified_regions()`로 자료가 있는 지역에만 적용한다.

### 13. 셸에서 `pgrep -f "01_collect.py"`는 자기 자신을 잡는다
대기 루프의 명령줄에 그 문자열이 들어 있어 **무한 대기**했다.
`pgrep -f "[0]1_collect.py"`처럼 대괄호를 쓴다.

## 지켜야 할 불변식

깨지면 표가 거짓말을 한다. 테스트로 강제돼 있으니 우회하지 마라.

1. **그룹 점수의 합 = 총점.** 모든 배점 항목이 정확히 한 그룹에만 속해야 하고,
   `config.validate()`가 로드 시 검사한다.
2. **표 헤더 수 = 셀 수.** 그룹 열은 헤더와 셀을 같은 `GROUPS` 배열에서 만든다.
3. **대표가는 단지 × 면적대별로 낸다.** 34㎡와 84㎡를 한 중앙값에 섞으면 의미가 없다.
4. **해제된 거래(`cdealType='O'`)는 제외한다.**
5. **지하철 시간에는 역까지 도보 시간을 더한다.** 자차는 집 앞에서 재므로,
   더하지 않으면 역세권이 과대평가된다.
6. **`config.yaml`은 절대 커밋하지 않는다** (API 키 4개가 평문으로 들어 있다).

## 작업 순서

1. 조건만 바꿀 때: `config.yaml` → `02_analyze.py` → `03_dashboard.py` (수집 캐시 재사용)
2. 수집 범위를 넓힐 때: `01_collect.py`부터. 시군구 코드는 응답을 먼저 확인
3. 대시보드를 고칠 때: 파이썬 테스트 + `node tools/verify_dashboard.mjs` **둘 다**
4. 푸시 전: `python3 -m pytest -q` 통과 확인. (한 번 깨진 채로 푸시한 적 있다)
5. 배포: `docs/index.html`을 커밋해야 사이트에 반영된다.
   Pages 빌드가 실제로 `built`인지, 라이브 내용이 새 커밋인지까지 확인하라
   (빌드가 `errored`인데 내용은 배포된 적이 있다)

## 결과를 볼 때 알아야 할 한계

- **지하철 소요시간은 추정치다.** 급행·배차간격·버스 환승 미반영.
  참고 구간 16곳 기준 평균오차 약 6분, 1호선 장거리는 최대 20분.
- **자차는 다음 평일 오전 8시 기준 조회값이다.** 주차 여건·유류비 미반영.
- **학군은 학업성취도가 아니다.** 공개가 중단되어 중·고 접근성 + 학원가 밀집도 근사치다.
- **경기 일부는 주상복합 판별이 안 될 수 있다** (분류 미확인으로 표시).
- 병원·마트는 대부분 만점에 가까워 순위에 거의 기여하지 않는다.
  실제 순위는 서울 접근성·역거리·학군이 정한다.
